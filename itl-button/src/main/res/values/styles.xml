<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AVLoadingIndicatorStyle">
        <item name="minWidth">30dp</item>
        <item name="maxWidth">30dp</item>
        <item name="minHeight">30dp</item>
        <item name="maxHeight">30dp</item>
        <item name="indicatorName">BallScaleIndicator</item>
        <item name="indicatorColor">@color/action_loading</item>
    </style>

    <style name="AVLoadingIndicatorDefaultStyle">
        <item name="minWidth">30dp</item>
        <item name="maxWidth">30dp</item>
        <item name="minHeight">30dp</item>
        <item name="maxHeight">30dp</item>
        <item name="indicatorName">BallClipRotatePulseIndicator</item>
        <item name="indicatorColor">@color/action_loading</item>
    </style>

</resources>